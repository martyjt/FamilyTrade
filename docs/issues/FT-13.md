# [FT-13] Build the private app shell and strategy/run controls

GitHub: https://github.com/martyjt/FamilyTrade/issues/14

Planning parent: https://github.com/martyjt/FamilyTrade/issues/1

## Current FT-02 packet precedence

The final reviewed contracts-v1 and this issue's concrete recipe below supersede
historical draft/pending-default language later in this packet. Readiness requires
both final-digest PASS reviews and publication recorded in FT-02 #3. Until that
gate passes, contracts are still in review. After it passes, FT-01 is ready only
for separate commissioning; other issues still require integrated dependencies,
baseline symbol/command binding and their external evidence. No implementation
or delivery authority is granted by this planning update.

## Problem

Users should configure and test supported strategies in the app without editing JSON or application code.

## Dependencies

- https://github.com/martyjt/FamilyTrade/issues/5 — [FT-04] Implement user identity, broker-account ownership and credential storage
- https://github.com/martyjt/FamilyTrade/issues/7 — [FT-06] Implement validated and versioned strategy definitions
- https://github.com/martyjt/FamilyTrade/issues/11 — [FT-10] Persist backtest runs and execute them as owned background jobs

## Scope

- Implement the authenticated React shell and a bounded rule builder for indicator/condition selection, entry/exit condition groups and reviewed parameter/risk controls. Use understandable nested forms rather than a general visual workflow graph.
- Show validation errors, immutable saved versions and explicit paper/live mode labels.
- Implement backtest date/data selection, submit/cancel controls and durable run status using existing application operations.
- Keep the existing AG Charts integration isolated for FT-14; avoid a general design-system or workflow-editor project.

## Acceptance criteria

- [ ] A user signs in, builds two different valid rule combinations (including one beyond provided presets), changes conditions/parameters, saves new versions and starts backtests entirely in the browser.
- [ ] Reloading the page restores run state; stale forms do not silently overwrite newer strategy versions.
- [ ] Invalid parameters and failed jobs have understandable messages; no live action is exposed.
- [ ] Cross-user resources are not visible through navigation or manipulated API requests.

## Validation

Browser tests of the complete editing/submission flow, error states, reload recovery and a second user's isolation.

## Exclusions

Trading charts, live-order controls, billing, public registration and arbitrary code editing.

## Dispatch profile and readiness

- Task type: implementation or evidence according to scope.
- Current dispatch state: **WAIT_DEPENDENCIES**. This is a planning snapshot, not permission to start.
- Worker: **gpt-5.6-terra / medium**.
- Reviewer A: **gpt-5.6-terra / medium**, fresh context; independently verify acceptance, fixtures and validation.
- Reviewer B: **gpt-5.6-sol / high**, fresh context; Browser acceptance, stale edits, useful errors, no hidden live actions and tenant/session boundaries.
- Owned seams: web/src app/auth/strategy/run forms and browser tests. These are proposed module locations; fill exact baseline paths/symbols before coding dispatch.
- Required packet before dispatch: Stable owned API schemas, error catalogue, validated strategy forms and minimal wireframes.

A written issue is not automatically ready. Before coding, link the reviewed contract version, concrete success/failure examples, exact inputs/outputs, allowed files and baseline validation commands. Verify dependencies are available. If a consequential policy remains open, return the specific gap to planning instead of asking a cheaper worker to invent it. Escalation requires a named unresolved problem.

## Orchestrated implementation and review

This issue is dispatched by a thin coordinator to the named worker subagent only after its readiness gate. The coordinator does not implement application code. Use one mutating issue/worktree at a time and fresh briefs with issue/contract/fixture pointers.

Freeze the candidate revision before independent Reviewer A and Reviewer B inspect it. Reviewers report the exact SHA, findings and PASS / CHANGES_REQUIRED / BLOCKED; they do not edit code. Return fixes to the original worker, then review the final changed revision again. Match CI and review evidence to the final PR head.

Follow the planning parent and docs/session-workflow.md. Each issue has a separate worker context; users need not create a new visible task for every issue. Use explicit model/effort settings rather than inheriting the coordinator model.

Issue text does not grant implementation, commit/push/PR, merge, deployment, purchase, provider-contact or live-order authority. The commissioning prompt supplies the approved queue and allowed delivery actions. Preserve unrelated changes and the unaccepted prototype. Do not start another issue outside that queue. Close only when the applicable planning output is reviewed or code is integrated and all external acceptance criteria are demonstrated.

## Overnight and rule-builder UI

Show the difference between entry/trading windows and overnight position permission. Present rule validation errors at the relevant condition/group. The exact supported catalogue and control behaviour must come from the reviewed FT-02 packet.

## Accepted paper controls and recovery — 2026-09-12

- Provide both distinct actions: **Pause new entries / keep managing exits**, and
  **Close positions and stop**. The second requests simulated liquidation under the
  reviewed fill model; it cannot report a position closed before an eligible fill.
- Persist operator intent separately from outage/deployment status. Manual pause or
  stop survives reconnect/redeploy and requires an explicit user action to reverse.
- Automatically resume previously running paper lanes only after repaired state,
  verified fresh eligible data and consistency checks. Stay paused with a visible
  reason when recovery fails. Respect sessions and overnight positions.
- Stale/offline feeds cannot produce fabricated current fills. A close-and-stop
  request can remain pending until executable; define handling of existing pending
  entries/exits and later user actions in the FT-02 transition table.
- Historical reconstruction is labelled separately from contemporaneous forward
  evidence. Repeated controls/recovery events cannot duplicate decisions or fills.

Acceptance includes each action with flat/open positions, stale feeds, closed
sessions, reconnects and redeployments. Verify UI and MCP share these operations,
and that a manual pause cannot be overridden by automatic recovery.

## Reversal–Breakout authoring workflow

After FT-02 (#3) freezes the packet, the app offers named baseline presets and
editable zone/setup/confirmation/entry/exit sections. Users can duplicate a baseline,
toggle reversal/breakout and add supported feature conditions, then launch separate
paper/backtest lanes without editing Python or Pine. Show pending retest/expiry and
why a condition rejected a setup. Keep source-default provenance distinct from the
FamilyTrade execution policy and immutable active versions.

## Accepted configurability clarification — 2026-09-12

The user accepted configurable trading behaviour through shared UI/MCP strategy
versions. Presets do not permanently fix trade direction, entry lifetime or filter
choices. FT-02 (#3) carries the full clarification and its two independent review
results. It supersedes fixed-sounding preset language in the earlier strategy draft;
the original source packet/review remains unchanged for provenance.

Setup expiry and entry-order lifetime are separate bounded configurable fields. The
one-bar entry lifetime is a proposed preset value, not a permanent engine constant.
Keep temporal causality, effect deduplication, ownership and higher-level caps
enforced. New algorithms/order models may need reviewed capability extensions.

This acceptance settles product direction, not every numerical default. Coding
readiness is unchanged. Next FT-02 work is the paper order/fill and risk contract,
including configurable activation/TTL, costs, ambiguous candles and overnight cases.

## Concrete contracts-v1 recipe

Exact fixture path: `docs/contracts-examples-v1.json`; case IDs: fully_serialized_rule_definition_crossover, invalid_rule_definition_has_typed_errors, strategy_edit_creates_version_and_running_lane_stays_pinned, access_two_user_denial, forward_delayed_bar_availability_and_exit_rule, exit_rule_unknown_does_not_close, generic_market_entry_resolves_relative_bracket. Assert their `expected` values, with the full semantics/crash tables above as additional boundary coverage.


Consume sections 2–3, 5–7, 11, 13. Owned seams:
`frontend/src/{api,auth,strategies,runs}/`, `frontend/tests/authoring.spec.ts`.
Use the shared validated API vocabulary; create no independent frontend formulas.
Browser input: login, editable supported tree/parameters and owned run request.
Output: immutable saved version, actionable validation, durable run status.
Test a non-preset rule combination, changed side and multi-bar TTL, distinct setup
expiry, stale form, session expiry, reload, invalid nested rule and second-user
crafted request. Run frontend lint/type/unit/build plus the Playwright command
established for this issue (`npm --prefix frontend run test:e2e -- authoring`).
No charting, general graph editor, live controls or paper-service implementation.

See docs/issue-recipes-v1.md for the common dispatch gate and docs/ft02-handoff-v1.md for precedence. Exact reviewed artifacts and digest are published in FT-02 #3; FT-01 must copy those bytes. Existing model profiles and exclusions still apply.
