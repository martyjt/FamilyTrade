# [FT-03] Prove broker and data access locally and on the Netcup server

GitHub: https://github.com/martyjt/FamilyTrade/issues/4

Planning parent: https://github.com/martyjt/FamilyTrade/issues/1

## Current FT-02 packet precedence

The final reviewed contracts-v1 and this issue's concrete recipe below supersede
historical draft/pending-default language later in this packet. Readiness requires
both final-digest PASS reviews and publication recorded in FT-02 #3. Until that
gate passes, contracts are still in review. After it passes, FT-01 is ready only
for separate commissioning; other issues still require integrated dependencies,
baseline symbol/command binding and their external evidence. No implementation
or delivery authority is granted by this planning update.

## Problem

IBKR is a candidate rather than a confirmed integration. Low subscription pricing alone does not establish retention rights, API capabilities or unattended session reliability.

## Dependencies

- https://github.com/martyjt/FamilyTrade/issues/2 — [FT-01] Bootstrap the repository and reproducible development tooling
- https://github.com/martyjt/FamilyTrade/issues/3 — [FT-02] Freeze the first-slice contracts and golden examples (planning)

## Scope

- Record the selected broker/data API, account eligibility, required subscriptions and confirmed permission for individual retention and automated use.
- Resolve a dated Micro Gold contract with authoritative tick, multiplier, currency, expiry and session metadata.
- Run a bounded historical request and ongoing subscription with explicit real-time/delayed status; record history limits and pacing.
- Demonstrate the supported login, reconnect and restart workflow on the actual Netcup server when available, including manual reauthentication steps.
- Repeat using independently entitled user sessions; do not share subscription credentials or market data.

## Acceptance criteria

- [ ] Dated evidence identifies the actual account/API path and provider terms, with no secrets in GitHub or logs.
- [ ] The chosen contract and observed historical/live sample are verified rather than inferred from synthetic fixtures.
- [ ] Observed CPU/RAM, interruptions and operator recovery steps are recorded for the intended server and session count.
- [ ] If rights, accounts or VPS access remain unavailable, report the exact unmet criterion and keep this issue open. A provider change with material price or architecture implications is returned for a user decision.

## Validation

Small authorised read-only provider calls plus a planned disconnect/restart exercise. Do not purchase subscriptions, contact providers or place orders without explicit user authority.

## Exclusions

Production ingestion, live orders, claims of indefinite unattended authentication and full broker adapter coverage.

## Dispatch profile and readiness

- Task type: implementation or evidence according to scope.
- Current dispatch state: **WAIT_DEPENDENCIES_AND_EXTERNAL_EVIDENCE**. This is a planning snapshot, not permission to start.
- Worker: **gpt-5.6-sol / medium**.
- Reviewer A: **gpt-5.6-terra / medium**, fresh context; independently verify acceptance, fixtures and validation.
- Reviewer B: **gpt-5.6-sol / high**, fresh context; Rights, actual contract, delayed versus real-time, independent sessions, supported login/recovery and observed limits.
- Owned seams: Bounded provider proof and evidence; no production adapter. These are proposed module locations; fill exact baseline paths/symbols before coding dispatch.
- Required packet before dispatch: FT-02 contract/metadata fields; bootstrap tooling; actual account and VPS access where required.

A written issue is not automatically ready. Before coding, link the reviewed contract version, concrete success/failure examples, exact inputs/outputs, allowed files and baseline validation commands. Verify dependencies are available. If a consequential policy remains open, return the specific gap to planning instead of asking a cheaper worker to invent it. Escalation requires a named unresolved problem.

## Orchestrated implementation and review

This issue is dispatched by a thin coordinator to the named worker subagent only after its readiness gate. The coordinator does not implement application code. Use one mutating issue/worktree at a time and fresh briefs with issue/contract/fixture pointers.

Freeze the candidate revision before independent Reviewer A and Reviewer B inspect it. Reviewers report the exact SHA, findings and PASS / CHANGES_REQUIRED / BLOCKED; they do not edit code. Return fixes to the original worker, then review the final changed revision again. Match CI and review evidence to the final PR head.

Follow the planning parent and docs/session-workflow.md. Each issue has a separate worker context; users need not create a new visible task for every issue. Use explicit model/effort settings rather than inheriting the coordinator model.

Issue text does not grant implementation, commit/push/PR, merge, deployment, purchase, provider-contact or live-order authority. The commissioning prompt supplies the approved queue and allowed delivery actions. Preserve unrelated changes and the unaccepted prototype. Do not start another issue outside that queue. Close only when the applicable planning output is reviewed or code is integrated and all external acceptance criteria are demonstrated.

## External account readiness

On 2026-09-12 the user confirmed they still need to open or fund an IBKR account. Keep account/subscription-dependent criteria open; no account opening, funding or subscription purchase is authorised by this issue.

## Accepted data direction — Level 2

The user prefers budgeting for COMEX Level 2 per user and collecting useful depth
early. Prove actual API entitlement, allowed automated use/retention, supported
levels/contracts, reset/reconnect behaviour and depth backfill availability. Do not
infer completeness or historical depth from a subscription. Report measured bytes/
events and gaps in a bounded authorised pilot once its duration/byte caps are fixed.
No purchase, account mutation or provider contact is authorised by this issue.
The candle-strategy proof is independent; a dedicated depth-recorder implementation
packet must be planned before that pilot can be dispatched.

## Concrete contracts-v1 recipe

Exact fixture path: `docs/contracts-examples-v1.json`; case IDs: access_two_user_denial, contract_expiry_close_and_no_roll, dst_session_mapping_uses_materialized_utc_calendar. Assert their `expected` values, with the full semantics/crash tables above as additional boundary coverage.


Consume sections 3–4, 8, 10 and 14. Create `docs/evidence/ft-03-provider.md` and, only
within commissioned proof scope, `tools/provider_probe.py` with no saved secrets.
Input: authorised per-user connection, actual dated contract and bounded request.
Output: redacted dated provider evidence for every FuturesContract field, calendar
revision, price/data mode, historical/live completion and correction behaviour,
pacing, independent entitlement/storage rights and supported VPS login/recovery.
Use the DST/expiry/ownership examples to explain expected mappings, but a synthetic
match is not provider proof. Validate probe bounds locally, then record exact
authorised calls and server measurements. Account/subscription/VPS availability
remain external gates. Depth collection needs its own separately bounded recorder
packet and authority; do not fold it into candle adapter implementation.

See docs/issue-recipes-v1.md for the common dispatch gate and docs/ft02-handoff-v1.md for precedence. Exact reviewed artifacts and digest are published in FT-02 #3; FT-01 must copy those bytes. Existing model profiles and exclusions still apply.
