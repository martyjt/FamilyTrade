# [FT-01] Bootstrap the repository and reproducible development tooling

GitHub: https://github.com/martyjt/FamilyTrade/issues/2

Planning parent: https://github.com/martyjt/FamilyTrade/issues/1

## Current FT-02 packet precedence

The final reviewed contracts-v1 and this issue's concrete recipe below supersede
historical draft/pending-default language later in this packet. Readiness requires
both final-digest PASS reviews and publication recorded in FT-02 #3. Until that
gate passes, contracts are still in review. After it passes, FT-01 is ready only
for separate commissioning; other issues still require integrated dependencies,
baseline symbol/command binding and their external evidence. No implementation
or delivery authority is granted by this planning update.

## Problem

The remote repository is empty and the local directory contains an interrupted, untested prototype. Establish a reviewed baseline so later sessions can work from Git rather than inheriting uncommitted experiments.

## Dependencies

- https://github.com/martyjt/FamilyTrade/issues/3 — [FT-02] Freeze the first-slice contracts and golden examples (planning). Consume the reviewed planning packets; they do not require an existing Git commit.

## Scope

- Review the local planning documents and, only under explicit commissioning authority, publish the exact reviewed documentation-only root before creating the focused bootstrap implementation PR. Follow docs/bootstrap-packet-v1.md; a PR cannot target an absent branch.
- Create the smallest Python backend and React/TypeScript workspace, dependency locks, formatting/type/test commands and CI.
- Provide development Docker conventions and a sample configuration with no credentials. Implement the reviewed framework choices and select compatible dependency versions using maintained primary documentation.
- Audit existing untracked prototype files individually; adopt only code justified by this issue and tested acceptance, without bulk staging or deleting unrelated files.

## Acceptance criteria

- [ ] A clean checkout installs from committed dependency locks and runs documented checks on Windows development and Linux CI.
- [ ] CI exercises meaningful bootstrap checks and fails for a deliberately broken check in a local verification.
- [ ] No secret, data archive, generated report or experimental implementation is included accidentally.
- [ ] Record the initial branch/base setup explicitly because origin has no default-branch commit yet. No parallel implementation worktrees start until a shared baseline exists.

## Validation

Demonstrate a clean-checkout setup, focused checks, container build and review of the complete staged diff. Do not call prototype output verified behaviour.

## Exclusions

Broker integration, strategy implementation, full UI, VPS deployment and Rust.

## Dispatch profile and readiness

- Task type: implementation or evidence according to scope.
- Current dispatch state: **REVIEW_GATED_COMMISSIONING**. This is a planning snapshot, not permission to start.
- Worker: **gpt-5.6-terra / medium**.
- Reviewer A: **gpt-5.6-terra / medium**, fresh context; independently verify acceptance, fixtures and validation.
- Reviewer B: **gpt-5.6-sol / high**, fresh context; Clean checkout installation, Linux CI and Windows development, no production secret/data inclusion.
- Owned seams: Repository foundation and dependency lock/build scripts. These are proposed module locations; fill exact baseline paths/symbols before coding dispatch.
- Required packet before dispatch: Reviewed FT-02 contracts and architecture/framework choices; a file-by-file prototype adoption list. Selecting compatible dependency versions and pinning them is this bootstrap worker's bounded task.

A written issue is not automatically ready. Before coding, link the reviewed contract version, concrete success/failure examples, exact inputs/outputs, allowed files and baseline validation commands. Verify dependencies are available. If a consequential policy remains open, return the specific gap to planning instead of asking a cheaper worker to invent it. Escalation requires a named unresolved problem.

## Orchestrated implementation and review

This issue is dispatched by a thin coordinator to the named worker subagent only after its readiness gate. The coordinator does not implement application code. Use one mutating issue/worktree at a time and fresh briefs with issue/contract/fixture pointers.

Freeze the candidate revision before independent Reviewer A and Reviewer B inspect it. Reviewers report the exact SHA, findings and PASS / CHANGES_REQUIRED / BLOCKED; they do not edit code. Return fixes to the original worker, then review the final changed revision again. Match CI and review evidence to the final PR head.

Follow the planning parent and docs/session-workflow.md. Each issue has a separate worker context; users need not create a new visible task for every issue. Use explicit model/effort settings rather than inheriting the coordinator model.

Issue text does not grant implementation, commit/push/PR, merge, deployment, purchase, provider-contact or live-order authority. The commissioning prompt supplies the approved queue and allowed delivery actions. Preserve unrelated changes and the unaccepted prototype. Do not start another issue outside that queue. Close only when the applicable planning output is reviewed or code is integrated and all external acceptance criteria are demonstrated.

## Concrete contracts-v1 recipe


Consume contracts sections 1–3 (framework/publication/serialization conventions)
and the complete reviewed document manifest.
Use `bootstrap-packet-v1.md` for the individually audited prototype files, exact
allowed paths, empty-origin publication boundary, success/failure examples and
Windows/Linux clean-checkout commands. Do not implement the domain contracts.
Acceptance output: reproducible shell/locks/CI, preserved exact packet bytes and
one reviewed PR after an explicitly authorised documentation root. This is the
first bounded commissioning candidate once both packet reviews pass.

See docs/issue-recipes-v1.md for the common dispatch gate and docs/ft02-handoff-v1.md for precedence. Exact reviewed artifacts and digest are published in FT-02 #3; FT-01 must copy those bytes. Existing model profiles and exclusions still apply.
